FROM ubuntu:19.10


RUN apt update -y && apt install -y \
    cmake \
    git \
    gcc \
    g++ \
    libboost-all-dev \
    nlohmann-json3-dev \
    libfmt-dev \
    libgtest-dev

# RUN mkdir build && cd build && cmake .. && cmake --build . -j
